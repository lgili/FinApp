Banking import examples used during Phase 2 validation:

- `sample_nubank.csv`: tiny hand-crafted CSV for quick smoke tests.
- `phase2_rules_dataset.csv`: dataset with 10 transactions where the stock rules achieve ≥90% suggestion coverage (used in acceptance tests).
